############################################################
# Example Environment Configuration (no real credentials)
# Copy to project root as `.env` and fill your values.
############################################################

# LLM Provider (OpenAI or Azure OpenAI)
# For OpenAI (api.openai.com) set only OPENAI_API_KEY.
# For Azure OpenAI, set endpoint + deployment + api version.

# OpenAI (non-Azure)
# OPENAI_API_KEY="<YOUR_OPENAI_API_KEY>"

# Azure OpenAI
OPENAI_ENDPOINT="<YOUR_AZURE_OPENAI_ENDPOINT>"           # e.g. https://your-resource.openai.azure.com
OPENAI_KEY="<YOUR_AZURE_OPENAI_KEY>"
OPENAI_DEPLOYMENT_NAME="<YOUR_AZURE_DEPLOYMENT_NAME>"    # e.g. gpt-4o-mini
OPENAI_DEPLOYMENT_VERSION="<YOUR_AZURE_API_VERSION>"     # e.g. 2024-08-01-preview
OPENAI_EMBEDDING_NAME="<YOUR_EMBEDDING_DEPLOYMENT_NAME>" # optional
OPENAI_EMBEDDING_VERSION="<YOUR_EMBEDDING_API_VERSION>"  # optional

# Data path used by the app and pipeline.
# Point to your local data directory (not included in repo)
# DATA_PATH="./data"
